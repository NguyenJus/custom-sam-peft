"""custom_sam_peft utility modules. Submodules are imported on demand.

This package marker is intentionally empty: callers import the specific
util they need (e.g. ``from custom_sam_peft.utils.huggingface import download_model``)
so heavy/optional third-party imports stay opt-in.
"""
